{
  "routes": [
    {
      "method": "GET",
      "path": "/vetements",
      "handler": "vetements.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vetements/count",
      "handler": "vetements.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/vetements/:id",
      "handler": "vetements.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/vetements",
      "handler": "vetements.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/vetements/:id",
      "handler": "vetements.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/vetements/:id",
      "handler": "vetements.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
